const numero1: number = 118;
const numero2: number = 56;

function sumar(a: number, b: number): number {
    return a + b;
}

const resultado = sumar(numero1, numero2);


console.log(`El resultado de sumar ${numero1} y ${numero2} es: ${resultado}`);